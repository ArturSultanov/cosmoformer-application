apiVersion: v1
kind: ResourceQuota
metadata:
  name: cosmoformer-rq
  namespace: cosmoformer-app
spec:
  hard:
    pods: "20"
    requests.cpu: "4"
    requests.memory: "8Gi"
    limits.cpu: "10"
    limits.memory: "32Gi"
